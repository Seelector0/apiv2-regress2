[pytest]
markers =
    not_parallel: mark test as not suitable for running in parallel

norecursedirs =
    .git
    .venv
    .idea

addopts =
   --tb=short -n auto --dist loadscope -m "not not_parallel" --alluredir=result --clean-alluredir


[pytest]
markers =
    not_parallel: mark test as not suitable for running in parallel

norecursedirs =
    .git
    .venv
    .idea

addopts =
   --tb=auto --showlocals --sugar -s -rs -n 8 --dist loadscope -m "not not_parallel" --alluredir=result --clean-alluredir


